import React from 'react';

import {
    Container
} from './style';

import ProgressBar from '../../components/ProgressBar';

export default () => {
    return(
        <Container>
            <ProgressBar/>
        </Container>
    );
}